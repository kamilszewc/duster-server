# MySQL
spring.datasource:
  url: jdbc:mysql://localhost:3306/dusterserver
  username: duster
  password: dusterPassword
spring.jpa:
  database-platform: org.hibernate.dialect.MariaDB10Dialect
  hibernate.ddl-auto: create-drop

## SSL setup
#server:
#  ssl:
#    key-store: /etc/letsencrypt/live/domain/keystore.p12
#    key-store-password: password
#    key-store-type: PKCS12
#    key-alias: alias
#    enabled-protocols: [TLSv1.2, TLSv1.3]
#  port: 443
server:
  port: 8081


# Credentials
credentials:
  admin-password: "password"
  token-secret: "23423rwvt64jOIQI0D345trertavevrael;,efpmkmcsmdsdsfpJ2ID3D#"

map:
  latitude: 53.01375
  longitude: 18.59814
  zoom: 12
  apikey: ""

#file-storage:
#  directory: "files"

# Logging setup
logging:
  file:
    name: dusterserver.log
  level:
    org.springframework: INFO
    com.essteyr.mammothmanager: DEBUG


# Internationalization
spring:
  messages:
    always-use-message-format: false
    basename: messages
    encoding: UTF-8
    fallback-to-system-locale: true
    use-code-as-default-message: false
  application:
    name: "Duster server"