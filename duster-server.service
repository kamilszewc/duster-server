[Unit]
Description=Duster server daemon
After=networking.service

[Service]
Type=simple
ExecStart=(path to duster)/dusterServerRun (path_to_config.yml)
Restart=on-failure

[Install]
WantedBy=multi-user.target
